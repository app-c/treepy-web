import styled from 'styled-components'
import { animated } from 'react-spring'

export const Container = styled(animated.div)`
  position: fixed;
  right: 0;
  top: 0;
  padding: 30px;
  overflow: hidden;
  z-index: 20;
`

export const Ani = styled(animated.div)``
